# Temporarily empty - Portfolio model disabled for new architecture

# This file exists to prevent import errors but doesn't define any models
# until the Portfolio feature is adapted to the new SHA256-based architecture